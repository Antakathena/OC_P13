version: '3.9'

services:
  app:
    image: oc_lettings_project:v3
    build:
      context: .
    volumes:
      - .:/oc_lettings_project
    ports:
      - "8000:8000"
    container_name: oc_lettings_container
    command: python manage.py runserver 0.0.0.0:8000